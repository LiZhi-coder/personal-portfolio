# 前端动态属性显示方案

## 🎯 目标
后端新增属性后，前端只需在**一个地方**配置，即可自动显示在所有相关区域。

---

## 📦 方案一：配置文件驱动（推荐）

### 1. 创建属性配置文件

```javascript
// src/config/propertyConfig.js

export const propertyConfig = {
  // 核心指标（显示在仪表板）
  core: [
    {
      key: 'n',
      label: '变量数',
      icon: '🔢',
      type: 'number',
      highlight: false
    },
    {
      key: 'hammingWeight',
      label: '汉明重量',
      icon: '⚖️',
      type: 'number',
      highlight: false
    },
    {
      key: 'nonlinearity',
      label: '非线性度',
      icon: '🎯',
      type: 'number',
      highlight: true  // 高亮显示
    },
    {
      key: 'algebraicDegree',
      label: '代数次数',
      icon: '🧮',
      type: 'number',
      highlight: false
    },
    {
      key: 'isBalanced',
      label: '是否平衡',
      icon: '⚖️',
      type: 'boolean',
      highlight: false
    },
    {
      key: 'isBent',
      label: 'Bent函数',
      icon: '🌟',
      type: 'boolean',
      highlight: false
    }
  ],
  
  // 基础性质
  basic: [
    {
      key: 'n',
      label: '变量数 (n)',
      category: 'basic',
      type: 'number'
    },
    {
      key: 'truthTable.length',
      label: '真值表长度',
      category: 'basic',
      type: 'number'
    },
    {
      key: 'hammingWeight',
      label: '汉明重量',
      category: 'basic',
      type: 'number'
    },
    {
      key: 'isBalanced',
      label: '是否平衡',
      category: 'basic',
      type: 'boolean'
    }
  ],
  
  // 代数性质
  algebraic: [
    {
      key: 'anf',
      label: 'ANF表达式',
      type: 'text',
      display: 'code'
    },
    {
      key: 'algebraicDegree',
      label: '代数次数',
      type: 'number'
    }
  ],
  
  // 密码学性质
  cryptographic: [
    {
      key: 'nonlinearity',
      label: '非线性度',
      type: 'number',
      highlight: true
    },
    {
      key: 'correlationImmunity',
      label: '相关免疫度',
      type: 'number'
    },
    {
      key: 'resiliencyOrder',
      label: '弹性阶数',
      type: 'number',
      transform: (val) => val >= 0 ? val : 'N/A'
    },
    {
      key: 'transparencyOrder',
      label: '透明度阶',
      type: 'number',
      transform: (val) => val != null ? val.toFixed(4) : 'N/A'
    },
    {
      key: 'isBent',
      label: '是否Bent函数',
      type: 'boolean'
    },
    {
      key: 'isRotationSymmetric',
      label: '是否旋转对称',
      type: 'boolean'
    },
    {
      key: 'sumOfSquareIndicator',
      label: '平方和指标',
      type: 'number'
    }
  ],
  
  // 频谱数据
  spectral: [
    {
      key: 'walshSpectrum',
      label: 'Walsh 频谱',
      type: 'array',
      display: 'data'
    },
    {
      key: 'autocorrelationSpectrum',
      label: '自相关谱',
      type: 'array',
      display: 'data'
    },
    {
      key: 'absoluteWalshSpectrum',
      label: '绝对Walsh谱分布',
      type: 'object',
      display: 'table'
    },
    {
      key: 'absoluteAutocorrelationSpectrum',
      label: '绝对自相关谱分布',
      type: 'object',
      display: 'table'
    }
  ]
}

// 导出分类
export const categories = [
  { id: 'basic', label: '基础性质', icon: '🔢' },
  { id: 'algebraic', label: '代数性质', icon: '🧮' },
  { id: 'spectral', label: '频谱分析', icon: '📊' },
  { id: 'cryptographic', label: '密码学性质', icon: '🔒' },
  { id: 'export', label: '数据导出', icon: '📁' }
]
```

### 2. 修改 Vue 组件，使用配置

```vue
<script setup>
import { propertyConfig, categories } from '@/config/propertyConfig.js'

// ... 其他代码保持不变 ...

// 工具函数：获取嵌套属性值
const getNestedValue = (obj, path) => {
  return path.split('.').reduce((current, key) => current?.[key], obj)
}

// 工具函数：格式化值
const formatValue = (value, config) => {
  if (config.transform) {
    return config.transform(value)
  }
  
  if (config.type === 'boolean') {
    return value ? '是' : '否'
  }
  
  return value
}
</script>

<template>
  <!-- 核心指标仪表板 - 动态生成 -->
  <div class="dashboard-overview">
    <h4>核心指标概览</h4>
    <div class="dashboard-grid">
      <div 
        v-for="prop in propertyConfig.core" 
        :key="prop.key"
        :class="['dashboard-card', { 'highlight-card': prop.highlight }]"
      >
        <div class="dashboard-icon">{{ prop.icon }}</div>
        <div class="dashboard-label">{{ prop.label }}</div>
        <div 
          class="dashboard-value" 
          :class="{
            highlight: prop.highlight,
            success: prop.type === 'boolean' && getNestedValue(analysisResult, prop.key),
            warning: prop.type === 'boolean' && !getNestedValue(analysisResult, prop.key)
          }"
        >
          {{ formatValue(getNestedValue(analysisResult, prop.key), prop) }}
        </div>
      </div>
    </div>
  </div>

  <!-- 标签页 - 动态生成 -->
  <div class="tab-container">
    <div class="tab-headers">
      <button
        v-for="cat in categories"
        :key="cat.id"
        @click="activeTab = cat.id"
        :class="['tab-button', { active: activeTab === cat.id }]"
      >
        {{ cat.icon }} {{ cat.label }}
      </button>
    </div>

    <div class="tab-content">
      <!-- 基础性质 -->
      <div v-if="activeTab === 'basic'" class="tab-pane">
        <div class="property-section">
          <h5>基础信息</h5>
          <div class="property-grid">
            <div 
              v-for="prop in propertyConfig.basic"
              :key="prop.key"
              class="property-item"
            >
              <span class="property-label">{{ prop.label }}:</span>
              <span class="property-value">
                {{ formatValue(getNestedValue(analysisResult, prop.key), prop) }}
              </span>
            </div>
          </div>
        </div>
      </div>

      <!-- 密码学性质 -->
      <div v-if="activeTab === 'cryptographic'" class="tab-pane">
        <div class="property-section">
          <h5>密码学指标</h5>
          <div class="property-grid">
            <div 
              v-for="prop in propertyConfig.cryptographic"
              :key="prop.key"
              class="property-item"
            >
              <span class="property-label">{{ prop.label }}:</span>
              <span 
                class="property-value"
                :class="{ highlight: prop.highlight }"
              >
                {{ formatValue(getNestedValue(analysisResult, prop.key), prop) }}
              </span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>
```

### 3. 新增属性只需修改配置

假设后端新增了 `averageSensitivity`（平均敏感度），只需：

```javascript
// 在 propertyConfig.js 中添加
export const propertyConfig = {
  core: [
    // ... 现有配置 ...
    {
      key: 'averageSensitivity',  // 后端返回的字段名
      label: '平均敏感度',          // 显示的中文名
      icon: '📊',                  // 图标（可选）
      type: 'number',              // 数据类型
      highlight: false             // 是否高亮
    }
  ],
  
  cryptographic: [
    // ... 现有配置 ...
    {
      key: 'averageSensitivity',
      label: '平均敏感度',
      type: 'number',
      transform: (val) => val?.toFixed(4)  // 保留4位小数
    }
  ]
}
```

**就这样！前端会自动在仪表板和详情页显示这个新属性。**

---

## 📦 方案二：后端驱动的元数据（更灵活）

如果希望完全由后端控制显示，可以让后端返回元数据：

### 后端返回格式

```json
{
  "data": {
    "n": 3,
    "hammingWeight": 4,
    "nonlinearity": 242,
    ...
  },
  "metadata": {
    "core": [
      {
        "key": "n",
        "label": "变量数",
        "type": "number",
        "category": "core",
        "order": 1
      },
      {
        "key": "averageSensitivity",
        "label": "平均敏感度",
        "type": "number",
        "category": "core",
        "order": 7,
        "highlight": true
      }
    ],
    "categories": [
      {
        "id": "basic",
        "label": "基础性质",
        "properties": ["n", "hammingWeight", "isBalanced"]
      }
    ]
  }
}
```

### 前端代码

```vue
<script setup>
// 动态配置从后端获取
const propertyMetadata = ref(null)

const analyzeFunction = async () => {
  const response = await axios.post('http://localhost:8080/api/analyze', requestData)
  analysisResult.value = response.data.data
  propertyMetadata.value = response.data.metadata  // 保存元数据
}
</script>

<template>
  <!-- 完全由后端配置驱动 -->
  <div class="dashboard-grid">
    <div 
      v-for="prop in propertyMetadata?.core" 
      :key="prop.key"
      class="dashboard-card"
    >
      <div class="dashboard-label">{{ prop.label }}</div>
      <div class="dashboard-value">
        {{ analysisResult[prop.key] }}
      </div>
    </div>
  </div>
</template>
```

---

## 🎯 两种方案对比

| 特性 | 方案一：前端配置 | 方案二：后端元数据 |
|------|-----------------|-------------------|
| **灵活性** | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐⭐ 最高 |
| **实现难度** | ⭐⭐ 简单 | ⭐⭐⭐⭐ 复杂 |
| **性能** | ⭐⭐⭐⭐⭐ 无额外请求 | ⭐⭐⭐⭐ 略有开销 |
| **修改成本** | 前端改配置文件 | 后端改返回格式 |
| **适用场景** | 属性相对固定 | 需要动态配置 |

---

## ✅ 推荐实施步骤

### 第一阶段（立即实施）
使用**方案一**，创建前端配置文件，让现有代码更易维护。

### 第二阶段（长期规划）
如果需要更高灵活性（如允许用户自定义显示），可以升级到**方案二**。

---

## 📝 总结

**当前问题：**
- 新增属性需要修改 3-5 处代码
- 容易遗漏，难以维护

**解决方案：**
- 使用配置驱动，只需在一处添加配置
- 前端自动生成所有显示界面
- 后端新增属性时，前端改动最小化

**需要我帮您实现这个动态配置系统吗？**
